import 'react-axios/dist/index.css'
import React from 'react';

const App = () => {
  return <div>To create library creat-react-library library_name then in src command npm start it will generate dist and to view our changes cd to example then run npm start </div>
}

export default App
